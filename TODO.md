# TODO

- [x] Diagnose why Dashboard “New” dropdown doesn’t open (outside-click handler closing immediately).
- [ ] Fix theme toggle so sidebar reflects theme (ensure Tailwind/utility classes use theme-driven colors, not hard-coded white-only classes).

- [ ] Implement code changes in `frontend/src/app/(dashboard)/layout.tsx`.
- [ ] Run frontend lint/build (or dev) to ensure no TS/React errors.

